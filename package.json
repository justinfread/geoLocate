{
  "name": "geolocate",
  "version": "1.0.0",
  "description": "Using node.js to create an express server, NeDB database and sending data back and forth from the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Fread",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nedb": "^1.8.0"
  }
}
